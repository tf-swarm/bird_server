#!/usr/bin/env python
# -*- coding=utf-8 -*-

from framework.context import Context
import random


class BirdComm(object):
    def add_buff(self, uid, gid, buff_config):
        return

    def deal_chips(self, gid, uid, cost, max_barrel_level):
        return


BirdComm = BirdComm()
